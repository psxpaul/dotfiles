[core]
    excludesfile = $HOME/.gitignore
[user]
    name = Paul Roberts
    email = $GIT_EMAIL
[diff]
    external = $HOME/bin/meldGit
    tool = gvimdiff
[color]
    ui = auto
[format]
    pretty = format:%C(yellow)%h%Creset -%C(red)%d%Creset %s %Cgreen(%ar) %C(bold blue)<%an>%Creset
[merge]
    tool = gvimdiff
[push]
	default = simple
